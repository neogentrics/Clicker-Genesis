# Clicker Genesis — Project Context

Bible-verse idle/incremental clicker for Android (Unity, URP 3D Core template). Goal: help players learn and memorize scripture through play. Micro-transactions exist but are never required for progress or content.

**Full design doc (source of truth, keep it updated as design evolves):** https://app.notion.com/p/3aead37812e881c99578d596f3a29921

## Guiding principle
Scripture text is never altered, paraphrased, or reinterpreted. Only unedited, recognized translations are used. This constraint overrides convenience in any implementation decision.

## Confirmed design (do not re-litigate without the user explicitly reopening it)

- **Currency:** Ink. Earned by tapping and by passive/idle upgrades ("scribes").
- **Core action:** spend Ink to buy the *next* verse in canonical order within your active book (no skipping within a book in v1). Buying reveals verse text + reference.
- **Screen split (CONFIRMED 2026-07-31, reopens/supersedes the old single-screen assumption):** tapping and buying are now separate screens — a Clicker screen (tap-for-Ink only) and a Buy Verse screen (spend Ink, verse reveal), navigated between via a button. This was an explicit, deliberate user decision, not incidental UI organization — the core loop's pacing now includes a screen switch between tap and buy.
- **Chapter bulk-buy:** sum of remaining verse costs in the chapter × 0.75 (25% discount).
- **Pricing curve:** `verse_cost(n) = base_cost × growth_rate ^ n`, where `n` resets per book (each newly unlocked book starts approachable, doesn't inherit endgame pricing). `base_cost`/`growth_rate` must be data-driven (config/ScriptableObject fields), never hardcoded magic numbers — they'll be rebalanced after playtesting.
- **XP/Leveling:** small XP per verse, larger per chapter, larger still per book completed. No separate "clicker leveling" or achievement subsystem in v1 — XP comes only from actions already in the loop.
- **Book unlocking:** player picks ONE starting book for free on first run (personalization hook). Locked into that book until prestige-eligible (a level threshold, reachable mid-book — does NOT require finishing the book). Post-prestige book choice is free-pick (not strict canonical order) within the currently-unlockable pool.
- **Testament scope (v1):** Old Testament only. No Testament selector/lock built yet — that pool is just "all OT books" for now. New Testament is a deferred content update; the original hard-lock idea (pick a Testament, stuck until it's 100% done) is a seed for later, not a v1 build target.
- **XP formula (CONFIRMED placeholder constants, implemented 2026-07-31):** content-completion based, not RPG/combat based. `XpConfig` ScriptableObject: xpPerVerse=1, xpPerChapterBonus=10, xpPerBookBonus=100. Level threshold curve: XP needed to go from level L to L+1 = `levelXpBase * L` (linear growth, levelXpBase=50) — cumulative XP at start of level L = `levelXpBase * (L-1) * L / 2`. `prestigeLevelThreshold`=5 (placeholder). All values explicitly placeholder pending playtesting, same rule as the Ink pricing curve — but now real, working, data-driven code, not just a formula on paper.
- **Clicker/Manager subsystem direction:** standard idle-game shape (Cookie Clicker / AdVenture Capitalist). "Scribes" = passive Ink/sec generators bought with Ink on an escalating curve. "Managers" = one per scribe type, gated by BOTH player level AND an Ink cost (REVISED 2026-08-04 — supersedes the earlier "auto-unlocks by level alone" description; managers now require an explicit purchase once level-eligible). Exception: Adam (first manager) stays free once level-eligible, no Ink cost — same "personalization hook stays reachable early" pattern as the free starting book. Exact rates/manager levels/costs still open (numeric only).
- **Scribes/Managers tab split (CONFIRMED 2026-08-04, BUILT 2026-08-04):** ClickerScreen's Scribes panel has a second tab, "Managers," listing all managers and their unlock cost/status (Locked-by-level / buyable / Owned). `ManagerListUI.cs` + `GameLoopController.BuyManager()`. Verified functionally via script test; awaiting user's own Play confirmation. Future scope (not v1): some managers Prestige-gated for certain books; paid (real-money) manager unlocks as a convenience shortcut, but per the IAP-never-required guiding principle above, any such manager must always also be obtainable through normal Ink/Prestige play — never purchase-exclusive.
- **Pending to-do, not yet built (relayed 2026-08-06 from a parallel "Bible indexing" planning session, user's explicit correction there):** `ManagerListUI.cs` row text is too verbose — drop the flavor-text description entirely, each row should show only the manager's name + their actual active bonus/perk. Also needs to leave room for additional perk lines once a manager gains a bonus from something other than its own level/Ink unlock (a Grace Skill Tree node, or the not-yet-built submanager system below) — surface those on the row too when they apply. Pick this up next time `ManagerListUI.cs` is open.
- **Books tab on Buy Verse screen (CONFIRMED 2026-08-04, still not built — the Grace Skill Tree it depends on is now built, this UI itself isn't):** third tab alongside Verses/Chapters, reading `PrestigeSkillSystem.IsBookUnlocked(resourceId)` to show which books the player has unlocked via the Book Progression skill-tree branch. Does not appear in the tab row at all until the player has prestiged at least once — not merely grayed out, fully hidden, same progressive-disclosure pattern as the ClickerScreen "Skill Tree" button.
- **Scribe/Manager theming (CONFIRMED 2026-07-31, Genesis locked in as the template):** each unlocked book gets its own scribe tier names (objects/motifs from that book) and manager names (real named characters from that book, each tied to something they actually do in the narrative — not arbitrary name-slapping). Genesis pilot (starting set, explicitly NOT exhaustive — user noted more scribes/managers can be added from Genesis later): Reed Pen → Papyrus Scroll → Stone Tablet → Oil Lamp → Ark's Manifest → Covenant Seal → Jacob's Ladder → Joseph's Storehouse (8 scribe tiers); managers Adam (Reed Pen), Noah (Ark's Manifest), Abraham (Covenant Seal), Jacob (Jacob's Ladder), Joseph (Joseph's Storehouse). This is content/naming theming layered on the shared v1 Ink economy — does NOT require the separate per-book currencies from the v1.x/v2 phase below. Other books' rosters get authored book-by-book as those books are actually built, not all up front.
- **Art direction (confirmed 2026-07-31):** two-layer system. Base UI = warm parchment & minimal ink (calm, legible, mobile-fast — used ~95% of the time). Accent layer = stained-glass *technique* (bold jewel-tone color blocks, dark linework, glow) reserved for reward moments only (verse reveal, chapter/book complete, prestige) — explicitly NOT literal cathedral/church imagery. Starting palette + typography proposal is in the doc folder overview (see below), not finalized art.
- **Layout requirement (confirmed 2026-07-31):** single build must be fully responsive — auto-detect mobile vs. desktop/tablet at runtime and reflow (portrait single-column on phone, wider multi-panel on desktop), not separate builds. Real Unity Canvas Scaler / safe-area / layout-group work, not just an art concern.
- **Prestige (CONFIRMED FULL SPEC 2026-08-03, BUILT 2026-08-05):** benchmarked against genre convention (Cookie Clicker, AdVenture Capitalist, Clicker Heroes, Egg Inc) before finalizing. Full spec lives on the Notion "Progression" subpage — summary (built behavior noted where it diverges from the original paper spec):
  - **Trigger:** reaching `prestigeLevelThreshold` (placeholder = 5), via a "Skill Tree" button on ClickerScreen (renamed from "Prestige" 2026-08-05 — the button only opens the Grace Skill Tree screen, where the actual Prestige/Reset choice is made, it doesn't itself prestige anything). Stays visible once the player has ever prestiged (`PrestigeCount > 0`), independent of level, so a level reset can never hide the only way back to spend Grace (real bug, fixed 2026-08-05). **Frequency:** no cooldown on the reset path (every reset cycle requires re-reaching the threshold again, since it resets Level/XP) — the free path does NOT reset Level/XP (REVISED 2026-08-05, explicit user correction — originally built to reset on every path, which stranded players with unspendable Grace once the button hid itself). **Currency:** single currency, "Grace," for v1.
  - *Default (free):* awards Grace = `floor(sqrt(LifetimeInkEarned)/50) + 10 + floor(versesUnlocked/5) + chaptersCompleted×2 + booksCompleted×10`. Nothing is lost — full Ink, upgrades, Level/XP, and all unlocked verses stay exactly as they are.
  - *Optional reset (opt-in):* player may voluntarily wipe Ink balance, Click Power level, scribe owned counts, AND Level/XP back to baseline for **2.5x** normal Grace.
  - **Hard rule, both paths:** a reset NEVER re-locks or removes verses already unlocked, and never touches Grace Skill Tree node ranks (permanent). Only numeric progress (Ink, upgrade levels, Level/XP on the reset path) can reset — never delivered scripture content or tree progress.
  - **Grace spending — real Grace Skill Tree (BUILT 2026-08-05, supersedes the lean-v1 flat-cost plan)**: 105 nodes across 9 branches, reached via the Skill Tree screen (`PrestigeScreen.unity` + `PrestigeScreenUI.cs`). 8 economy branches (Ink Flow, Steady Hand, Overseer's Wisdom, Illuminated Pages, Scribe's Diligence, Grace of Memorization, Swift Unlock, Manager's Calling), each an 8-node chain (3× rank-1 filler, 2× rank-3, 2× rank-5, 1× rank-1 capstone), wired into the real economy math via `GameLoopController.EffectiveInkPerSecond`/`EffectiveTapAmount`/pricing/Grace-preview/`EffectiveManagerLevel`. A 9th Book Progression branch unlocks the 38 remaining OT books individually (canonical order) plus a 3-node New Testament gate cluster (Gospel Threshold → Apostolic Path → Revelation's Veil, steep Grace cost, real-money alternative planned but not wired). Radial layout: branches as spokes from a central "Grace" hub, connecting lines, per-branch icon theming (Homeless/Item_Icons pack), zoom in/out, node brightness tracks OWNERSHIP not just prerequisite-unlock (a node stays dark until at least rank 1 is actually bought, regardless of whether its prerequisite is satisfied — real user correction 2026-08-05, first pass conflated "available to buy" with "already lit"). Full branch/node detail in the offline doc folder's Grace-Skill-Tree.html. The old automatic "+1% Ink per Grace ever spent" auto-bonus is removed (superseded, not stacked).
  - **Visual polish + layout redesign pass (2026-08-05, same day as the initial build).** Header/chrome themed to match every other screen: Pause button got the real gear-icon sprite (was plain text "Menu"); Grace readout got a dark background panel (was unreadable floating text); Prestige/Reset/Back header buttons got the same wooden sprite as everywhere else; Back button shrunk to a small "<" icon with a "Back" hover tooltip; skybox set to match. **Book Progression branch layout rebuilt** (user's explicit correction — the original build gave it a 9th spoke slot among the economy branches with a per-node angular-drift hack to fake a spiral arm, which read as "the books of the bible just kinda curve up to the right and then to the left... nodes are too close to each other, can't even read the descriptions"): now a full outer ring at a fixed radius outside the economy wheel's reach, all 41 book/gate nodes spread evenly across 360° and connected in canonical order, framing the 8 economy spokes like a wheel's rim — economy branch spacing also widened (base radius 220→320, ring step 150→210) so neighboring branches' labels stop overlapping. **Zoom In/Out buttons** were built with a broken sprite path (missing the double space in `UI board Small  stone.png` — this asset pack's filenames are inconsistently spaced, a recurring trap) that silently fell back to Unity's default white sprite with no glyph at all; fixed the path and added real "+"/"-" labels, since the buttons never had any text on them to begin with.
  - **Two new UX features (2026-08-05, user's own ideas).** A persistent **description box** sits between the Grace readout and the tree — hovering any node (even locked/unaffordable ones) shows its description there instead of relying on a floating tooltip aimed precisely at a small circle; every other button on the screen still uses the regular floating tooltip. Clicking a node no longer spends Grace instantly — a themed **Yes/No confirmation popup** shows the exact rank and cost first ("Buy Ink Flow I (rank 1/1) for 5 Grace?") before anything is spent.
  - We deliberately diverge from every genre example here: they all wipe everything except the prestige currency on reset. Ours never touches unlocked verses on either path — mission-driven adaptation, not a gap.
  - **Progressive node VISIBILITY, not just brightness (CONFIRMED 2026-08-06, NOT YET BUILT — real correction/addition to the built behavior above).** The existing "brightness tracks ownership" fix (bug #97) only handles nodes the player can already SEE. What's still missing: unowned nodes shouldn't render at all until their prerequisite is owned - not grayed out, not visible-but-locked, literally not drawn. Mechanic, in the user's own words: buying Core reveals only the very first node on each of the 9 spokes/ring-start - nothing else. Buying that first node reveals the next node after it (still locked/grayed until affordable or bought), and so on down each branch. The point is players never see the tree's full scale up front - if more nodes get added in a future update, it reads as "there's suddenly 5 more skills" rather than "the tree always looked like this." Needs a real change to `PrestigeScreenUI`'s tree-building/rendering logic (currently draws every node from the config at once); connecting lines presumably need the same treatment (a line to a not-yet-revealed node shouldn't draw either). Scoped as its own follow-up, not started.
- **Per-book currency/theming:** phased. v1 = single shared Ink, cosmetically reskinned per book (color/label only, e.g. "Genesis Ink"). v1.x/v2 = real separate non-transferable per-book currencies + unique clicker sets. Don't build the full N-economy system until the base loop is proven.
- **Translations (v1):** KJV only. NIV and Amplified are explicitly on hold/deferred to a future update (confirmed 2026-07-31) — not a pending decision to chase, not being built around at all right now. Revisit licensing when that update is actually scoped.
- **Versioning:** `X.Y.Z` — X = official release marker (0 = pre-release), Y = feature/update bump (whether or not shipped live), Z = patch/bugfix only. Log changes in the Notion Changelog subpage.

## In progress — v0.3 push (spec'd 2026-08-05, landing in phases, full plan in session transcript)
Large batch spec'd out from a single detailed user message, approved via Plan Mode before any implementation started. Landing in phases (A-F), each verified live before the next starts. Notion is disconnected this session (MCP not authorized) — this note is standing in until it can be synced there.
- **Phase A (BUILT 2026-08-06):** fixed "Unlock Chapter" button's mid-word text-wrap bug (bug #48, TMP auto-size); removed the redundant Tap-button tooltip, folded into Upgrade Tap's tooltip (bug #50); added mouse scroll-wheel zoom to the Skill Tree; node hover now shows live Grace cost alongside its description; added tooltips to the Prestige/Reset header buttons, visible while locked (bug #49); mitigated an unreadable top-right button-text report (bug #52 — root cause was node icons sitting behind the description box near the top of the tree at default view; shrank the box and added tree clearance).
- **Phase B (BUILT 2026-08-06):** added a central "Core" node ("Grace Awakened," gold star icon) to the Grace Skill Tree that every branch requires at rank 1 before anything else is buyable (all 8 economy spokes AND the Book Progression ring's first node — confirmed with the user, nothing is buyable until Core is bought first). Replaces the old inert "Grace" text-in-a-box hub visual. Retuned the cost curve: base costs ~1.5x, per-rank growth 1.6x→2.0x. Verified live: branch roots unbuyable before Core, all unlock together once Core is bought.
- **Phase C (BUILT 2026-08-06):** after a player's first Reset-Prestige — verse/chapter/book XP permanently become 10/25/150 (up from 5/10/100, user's numbers after a self-correction mid-spec, confirmed as "you decide, make it reasonable"); tapping and scribe purchases start granting XP for the first time (5 and 1 respectively, were 0 either way); a permanent stacking base passive Ink/sec bonus (+0.5/sec per reset performed, folded into `GameLoopController.EffectiveInkPerSecond`); a permanent stacking book-completion Ink multiplier (2× after the first reset, +1× per additional reset, once at least one book is complete). Every branch capstone and the whole New Testament gate cluster now require `requiresResetPrestige` (checked against `PrestigeSystem.ResetPrestigeCount > 0`) on top of their normal prerequisite chain — shown as "(Requires Reset)" instead of generic "(Locked)". `PrestigeSystem.PrestigeCount` split into `FreePrestigeCount`/`ResetPrestigeCount` (still sums to the old combined value for the one existing call site). Free-prestige stays Grace-only with no permanent bonuses beyond skills bought. Verified live via script test: XP/income math checked before and after a real reset, matched expected values exactly.
- **Phase D (BUILT 2026-08-06):** real Stats screen wired to the Pause Menu's "Stats" button (was a disabled stub) — swaps in over the same panel footprint (`PauseMenuController.MainPanel`/`StatsPanel`) rather than a separate scene, showing lifetime Ink earned/spent (`InkWallet.TotalSpent` is new), Grace balance/earned/spent, Free vs. Reset prestige counts, skills bought (`PrestigeSkillSystem.PurchasedNodeCount()`, new), managers bought (`ScribeSystem.UnlockedManagerCount()`, new), verses/chapters/books completed, and a placeholder for Store boosts. A scrollable "Permanent Upgrades" panel on ClickerScreen (right of the Tap bottle, real screen-space required careful world-corner measurement since the tap area isn't centered on the full canvas the way it looks) lists every skill-tree node owned via `PrestigeSkillSystem.GetAllPurchased()` (new). `ClickerScreenUI.ClickingPowerLabel` is now driven by a per-frame `Update()` (not the OnStateChanged-driven `Refresh()`, to avoid a race) — a rolling 2-second tap-timestamp window blends into the passive rate while actively tapping and decays back down a couple seconds after stopping; cosmetic only, doesn't change actual Ink awarded (`TapForInk()` is untouched). Verified live: real Stats numbers after test purchases/prestige, upgrades list updates on purchase, Clicking Power visibly climbed during a tap burst (screenshot showed "5.1 Ink/s") and decayed back down.
- **Phase E (BUILT 2026-08-06):** XP bar shine rebuilt (bug #51). Real root cause was two-fold: the old shine was a single plain rectangular Image (square corners), clipped by a `RectMask2D` on Fill - which clips to a bounding rectangle only and ignores the rounded sprite underneath, so even a correctly-contained shine never matched the bar's actual silhouette. Rebuilt: a runtime-generated diagonal two-tone stripe texture (green + white, no translucent blending - the old version's blended white-over-green read as a muddy third color) drawn via `RawImage` and scrolled sideways through its `uvRect` (real barbershop-pole motion, no shader needed), clipped to Fill's actual rounded shape by swapping `RectMask2D` for a real `Mask` component on Fill using Fill's own sprite as the stencil. Verified live on both XP bars (ClickerScreen and BuyVerseScreen).
- **Phase F (BUILT 2026-08-06, largest scope, landed as three sub-phases):** real Books tab with book-switching.
  - **F1 — data layer:** `GameLoopController`'s single global verse cursor became a `Dictionary<string, BookProgress>` (new `Assets/Scripts/Data/BookProgress.cs`) keyed by book resource id — every book ever touched gets its own `NextVerseIndex`/chapter-gate/chapter-count. The public API (`Verses`, `NextVerseIndex`, `ChaptersCompletedCount`, `BooksCompletedCount`, etc.) kept the exact same name/type via computed proxies to the active book's record, so every existing call site (pricing, chapter gate, Grace preview, XP awards) kept compiling and behaving identically for the Genesis-only case — verified via a live regression test before F2 started. `ChaptersCompletedCount`/`BooksCompletedCount` are now lifetime sums across every book ever touched, not separately-incremented fields. Scribe/manager unlock gating deliberately keeps reading Genesis' progress specifically via a `GenesisNextVerseIndex` computed property, not whichever book is currently active — the scribe roster (Reed Pen→Joseph's Storehouse) is Genesis-only themed content, so reading the active book's cursor instead would make already-unlocked scribes look locked again the instant a freshly-switched-to book's own cursor starts at 0.
  - **F2 — switching logic:** `CanSwitchToBook(resourceId)`/`SwitchActiveBook(resourceId)` enforce "book N+1 needs book N finished" — true only if the book is unlocked via the Grace tree (or it's Genesis, always free) AND the immediately-preceding canonical book is complete. Canonical OT order now lives in one shared place, new `Assets/Scripts/Data/CanonicalBookOrder.cs` (runtime-accessible, since the Editor-only Grace tree generator can't be referenced from runtime code) — `PrestigeSkillTreeGenerator.cs`'s own book-order array was refactored to derive from this same source instead of duplicating it, so the two can never drift out of sync.
  - **F3 — Books tab UI:** third tab on `BuyVerseScreenUI` alongside Verses/Chapters (new `BookListUI.cs`, same row-list pattern as `ManagerListUI`/`ChapterListUI`), hidden entirely (tab button + list) until the player has prestiged at least once — same progressive-disclosure pattern as the ClickerScreen Skill Tree button. Lists all 39 OT books with live status (Active/Complete/Locked/"Finish X first"/Switch) and a Switch button per row; the shared Buy/Multiplier controls hide entirely on this tab since switching happens per-row instead.
  - Verified live end to end via script test: unlocked Exodus through the skill tree, confirmed the switch was blocked while Genesis was incomplete, forced Genesis complete, switched into Exodus (cursor correctly reset to 0, `Verses.BookName` updated), confirmed Genesis' own progress was preserved (not wiped) and its row now offered "Switch" back.
  - **Self-caught follow-up bug fix (2026-08-06, same day):** `ScribeListUI.cs`/`ManagerListUI.cs` were still gating scribe/manager unlock display on `Controller.NextVerseIndex` directly - after F1 that property became the ACTIVE book's cursor, not Genesis' specifically, so switching away from Genesis would have made the Scribes/Managers tabs show already-unlocked content as locked again the instant the new book's cursor started at 0. `GenesisNextVerseIndex` made public on `GameLoopController` and both UI scripts switched to it. Verified live: switched active book to Exodus, confirmed Adam's Managers-tab row still read "Owned"/active throughout.
  - **`ManagerListUI.cs` row text simplified (2026-08-06, per the ManagerListUI to-do above):** dropped the flavor-text description entirely - each row now shows only the manager's name plus its actual active bonus line(s) (e.g. "+25% Reed Pen output"), with a second line appended when Overseer's Wisdom (Grace Skill Tree) boosts it (e.g. "+2% from Overseer's Wisdom") - also fixed the row to show the real skill-boosted total instead of just the manager's own base percentage, which had silently drifted from what `GetTierInkPerSecond` actually paid out. Nothing shown until the manager is actually active; built as appendable lines so a future submanager perk can slot in later. Verified live via script test with a real Overseer's Wisdom purchase.
- **Real-playtesting bug batch (2026-08-06, from the user's own Play session after F/ManagerListUI landed) - all fixed and verified live via script test, awaiting the user's own re-test confirmation:**
  - **XP bar shine direction/speed/color:** the shine was scrolling backwards (`uvRect.x` increasing samples texture further right, which visually scrolls the pattern LEFT, not right) - fixed by subtracting the offset instead of adding it. Sped up (`ScrollSpeed` 0.6→1.0) and swapped the highlight tone from a near-white `(0.85,1,0.9)` (read as plain white against the green fill) to a warm gold `(1,0.84,0.35)` for real two-tone contrast.
  - **Auto-Buy Reserve tier ceiling:** `GameSettings.ManagerAutoBuyReserveTiers` was a fixed 5-tier list capping at 1M and wrapping back to 0 - real Ink balances from actual play outgrew it fast. Extended to 11 tiers up to 1T. Still a fixed preset list, not the "scale relative to lifetime-max Ink held" version noted in the Roadmap as a future Statistics-screen-driven feature.
  - **`ScribeMilestoneBoost` skill (Scribe's Diligence branch) never showed on scribe rows:** the skill effect was correctly wired into `EffectiveInkPerSecond` (global scalar) but `ScribeListUI`'s own "×X.X milestone bonus!" row text never reflected it, reading as if the skill did nothing. Added a second line ("+N% from Scribe's Diligence") mirroring the pattern already used for Overseer's Wisdom on manager rows.
  - **Books tab locked-row tooltip:** added `HoverTooltip` to every `BookListUI` row (enabled only for the "Locked" - not-yet-unlocked-via-skill-tree - state) reading "Can be unlocked in the Skill Tree after a prestige is done."
  - **Skill Tree scroll-wheel "just scrolls instead of zooms":** root cause was `TreeScrollView`'s `ScrollRect` set to `MovementType.Clamped` - since `PrestigeScreenUI.Zoom()` changes `Content.localScale` directly (a custom scale-based zoom the ScrollRect knows nothing about), Clamped's per-frame bounds-enforcement was repositioning `Content.anchoredPosition` to compensate for the new scale, which visually read as panning. Switched to `Unrestricted` - verified live that `anchoredPosition` now stays fixed across zoom clicks (was shifting before the fix, confirmed via direct before/after read). `scrollSensitivity` was already 0 (not the actual cause, despite looking like an obvious suspect).
  - **Core hub node showing a plain circle instead of a star:** the Phase B note said "gold star icon" but no icon was ever actually wired - `PrestigeScreenUI.LookupIcon("Core")` had no matching `BranchIconEntry`, so the Core node rendered as just its bare colored circle. Found a real star sprite already in the project (`UI button pack 3/white icon/star.png`, needed the usual Texture→Sprite reimport) and added the missing `BranchIconEntry`. Verified live via screenshot.
  - **Permanent Upgrades panel text overflow:** title ("Permanent Upgrades") wasn't reliably fitting its box at a fixed font size - gave it auto-sizing (14-26) as a safety net. Body list text was left-aligned in a box whose actual width IS correctly matched to the parchment (a `VerticalLayoutGroup` with `childControlWidth=true` was already sizing it right - the overflow was a text-wrapping/alignment issue, not a box-sizing one) - set `TextWrappingModes.Normal`, center alignment, bumped font size 20→22, and enabled Bold. Verified live via screenshot with real multi-entry content.
  - **Confirmed NOT bugs, just undiscovered:** the Book Progression ring's node buy button - Exodus's node has a real, interactable `Button` wired through the same click→confirm-popup flow as every other skill node; the user just hadn't clicked one yet. No fix needed.
  - **Still-open items from the same feedback, not yet built:** managers still auto-buy purely on level/Ink/scribe-tier-unlock, not per-character verse-reference gating (tracked as task #120, blocked on CharacterIndex data); a "Finish the rest of this book" bulk-buy button on `BuyVerseScreenUI` (user's ask, tied to the not-yet-built Talents dual-currency pattern - Ink price should be steep-but-not-absurd, e.g. roughly matching the last real verse's cost rather than the book's literal total, with a parallel Talents-priced option once that currency exists); the Grace Skill Tree progressive-visibility redesign (task #119, unrelated to this batch, still pending); an updated Windows desktop build reflecting all of this (user wants to test outside the Editor once one exists).

- **OVERNIGHT AUTONOMOUS SESSION, COMPLETE (started 2026-08-06 late night, checkpoint updated same night once the task list was exhausted)** - user gave an explicit "work on this until it's completely done... this will be your task for tonight" and went to sleep, with a mid-session follow-up asking this file specifically be kept current for the parallel Bible-indexing session. Every item from that night's task list (#119-135) is now either BUILT or answered/logged as a recommendation - nothing was left un-started. Nothing below is confirmed by the user yet except where noted - "Built" means code-complete and verified live via script-execute/screenshot, but the user has not personally re-tested any of it.
  - **BUILT: Grace Skill Tree progressive node visibility (task #119/#123, real user re-ask - "it seems you didn't do the thing I mentioned before... the whole screen should be blank" unless prerequisites are bought).** Added `PrestigeSkillSystem.PrerequisiteSatisfied(node)` (prerequisite-rank check only, deliberately NOT including the `requiresResetPrestige` gate - see its doc comment for why) and `PrestigeScreenUI.ApplyNodeVisibility()`, called once after `BuildTree()` and every `Refresh()`. Every node (and its incoming connector line, now tracked via `NodeVisual.IncomingLine`) is fully `SetActive(false)` until its prerequisite chain is satisfied. Verified live via screenshot: a fresh session shows ONLY the Core node; buying Core reveals exactly the first node on all 9 spokes/ring-start and nothing else.
  - **BUILT: Owned/active glow on Skill Tree nodes (task #124).** Added a real Unity `Outline` component per node (gold, `(1,0.95,0.55,0.95)`, 5px offset), toggled on once `rank > 0` - distinct from the existing "affordable" brightness step. Verified live via screenshot on Core.
  - **BUILT: Skill Tree scroll-wheel zoom regression (task #121) - real architecture fix, not a settings tweak.** The `Clamped`→`Unrestricted` fix from the earlier batch (line 61 above) stopped the zoom-vs-pan fight but then broke wheel-zoom outright per a fresh user report. Root-caused as ScrollRect fundamentally not being designed for external code driving `Content.localScale` - rather than keep tuning ScrollRect settings against that, wrote a new tiny `PannableArea.cs` (`IBeginDragHandler`/`IDragHandler`, directly adjusts `Content.anchoredPosition`) and fully disabled `TreeScrollView`'s `ScrollRect` component (`enabled=false`). Pan (drag) and zoom (wheel, in `PrestigeScreenUI.Update`) are now two fully independent systems that structurally cannot interfere with each other again. **Cannot verify the actual mouse-wheel/drag gestures via script-execute (no way to simulate real OS input) - needs the user's own hands-on test.**
  - **BUILT: Skill Tree Zoom In/Out buttons re-themed (task #122).** Were using the flat gray `UI board Small stone` sprite (blended into the gray backdrop, per the user's report); switched to the same warm `TextBTN_Medium` wooden sprite + cream label color used by the Pause/gear button right next to them and every other themed button in the project. Verified live via screenshot - now clearly visible against the background.
  - **BUILT: Book Progression ring brought closer to center (task #128).** Gap from the economy wheel's outer edge reduced 380→120 units. Deliberately kept the RING shape rather than reverting to a literal interleaved spiral - a tight spiral is the exact layout bug #47 replaced with this ring in the first place (it was explicitly disliked: "curves up to the right and then to the left... can't even read the descriptions"). Flagged for the user to confirm this tradeoff is right, or say if they want the literal spiral-through-branches behavior instead. Verified live via screenshot: Exodus's node is now visible in the same zoomed-out view as the economy spokes.
  - **BUILT: Scribe row description text removed (task #125), matching the ManagerListUI fix from earlier tonight.** `ScribeListUI.cs` no longer shows `def.flavorText` - stats only (milestone bonus, Scribe's Diligence line, manager-active line), built as a joined line list instead of string concatenation so no stray leading blank line when the first line is conditionally absent.
  - **BUILT: "Active Upgrades" panel reworked (task #126) - real user re-ask, this panel was still a problem after the earlier pass.** Renamed from "Permanent Upgrades," title now auto-sizes (14-26pt) as an overflow safety net, moved down 16 units for top clearance. Panel widened 320→390 (extended leftward only, right edge unchanged since it was already tight against the canvas edge - left edge stops well short of the Tap button's own measured right edge, no new overlap). Converted from a single multi-line `TMP_Text` blob into real per-entry rows (new `PermanentUpgradesListUI.cs`, same list pattern as `ScribeListUI`/`ManagerListUI`/`BookListUI`), each with its own `HoverTooltip` (`PreferredDirection = Left`, per the user's explicit ask) showing that node's real description. Has cheap change-detection (a signature hash) so it does NOT rebuild every frame despite `OnStateChanged` firing continuously from passive Ink ticking - would have reintroduced the exact bug #22 lag class of problem otherwise. `ClickerScreenUI` no longer holds a `PermanentUpgradesLabel` reference at all; the new component is fully self-contained. Verified live via screenshot with real multi-node content and confirmed each row's tooltip text/direction directly.
  - **BUILT: Stats screen full bonus breakdown (task #127).** New `PauseMenuController.BuildBonusBreakdown()` appended into the existing Stats panel text - a "Current Output" section (live `EffectiveTapAmount`/`EffectiveInkPerSecond`) and an "Active Bonuses" section listing every non-zero skill/reset/progress bonus by name (Click Power, Ink Income, Manager Bonus, Progress Multiplier boost, Milestone Bonus, Grace Gain, Pricing Discount, Manager Unlock Level discount, the flat Reset base-Ink bonus, the book-completion multiplier), each line only shown when actually non-zero. Verified live via reflection-invoked `RefreshStats()` after real test purchases.
  - **BUILT: GitHub Issues set up as the real bug-tracking record (task #133), user's explicit "before you do anything else" priority.** All 52 numbered bugs in Bug-Tracker.html now have a matching GitHub Issue on `neogentrics/Clicker-Genesis` - closed for the 24 user-confirmed Fixed bugs, open for the 28 still-unconfirmed Open ones (including "pending confirmation" items, since those aren't user-confirmed yet, matching the existing "only the user's word moves a bug to Fixed" rule). The 6 issues that already existed from an earlier partial attempt were corrected to match (5 closed, 1 left open). Going forward: closing a GitHub Issue = moving that bug to Fixed in Bug-Tracker.html - keep both in sync on every future confirmation, per the user's new standing rule ("we're gonna do that for any GitHub repository moving forward").
  - **ANSWERED (task #129): does Ink transfer between books, do scribes/managers keep earning?** Yes to both - already the built Phase F behavior, not a new decision. Full reasoning logged in Roadmap.html's "Waiting on a user decision" section (now marked answered) since the user asked this without realizing Phase F had already settled it.
  - **RECOMMENDATION GIVEN, NOT YET ACTIONED (task #130): scribe "Owned: N" vs. a level-based system.** Recommendation: keep the owned-count mechanic as-is (it's already functionally a level system via `MilestoneCurve.cs`'s breakpoint multipliers), just cosmetically relabel "Owned: N" to "Level N" if the user wants the wording - a real mechanical rework would mean re-deriving every tuned cost/output number for no clear player-facing gain. Full writeup in Roadmap.html; needs the user's yes/no before touching `ScribeListUI.cs`.
  - **BUILT: Store design brief - shape/color/feel (task #131).** New "Visual design brief" section in Store-Monetization.html: the Store is a fourth Pause Menu panel (not a new scene, same pattern as Stats), reserves the stained-glass accent layer for this screen specifically (parchment base + jewel-tone left-edge accent bars per currency, Talents icon allowed to shimmer at rest), stays abundant/instructional rather than scarcity-pressured (every Talents row shows its free/Ink/Grace equivalent alongside it, no countdown timers or urgency badges), and specs a coin-silhouette Talents icon distinct from Ink/Grace. Idea capture only, nothing built in-engine.
  - **CHECKED (task #134): parallel session's `CharacterIndex/Genesis.json` output.** Read-only status check - it's a substantial draft (19 scribe tiers with real managers, up from the current live 8; ~70 characters with genealogy/verses/ages/fun facts; ~20 submanagers with gameplay-perk flavor tags). Not touched or wired into gameplay - both the submanager system and the Genealogy screen it feeds are explicit v3/v4 backlog, and per this file's own rule, real scripture-accurate theming needs the user's read-through before it's assigned, not unilateral invention. Full note in Roadmap.html.
  - **ANSWERED (task #135): is there a dedicated task-status doc in the offline folder?** No separate file exists or was created - Roadmap.html already serves exactly this purpose ("What's actively in flight, what's deliberately parked, and what's waiting on a decision only the user can make"), plus this session's own tracked task list (#1-135). Pointed the user at Roadmap.html rather than adding a duplicate file.
  - **Version bumped to 0.3.0** (`ProjectSettings/ProjectSettings.asset` `bundleVersion`, was 0.2.17) per the user's explicit overnight instruction ("we can officially move to update three"). Judgment call, flagged in both Roadmap.html and Bug-Tracker.html: this overrides the earlier self-imposed gate ("don't bump to 0.3.0 until every open bug is confirmed"), which was NOT independently satisfied - treated as a direct user override, not a claim the gate's conditions were met. If "update three bug fix two" meant a specific different patch number, correct it.
  - **Still open, deliberately not started tonight:** task #120 (manager unlock gated on a character's actual first-mention verse - blocked on reviewing CharacterIndex data together with the user, not a code blocker); bug #32 (Resolution Change button needs ~3 clicks - needs the user's own real-device retest, can't be diagnosed further from the Editor); the real audio system (task #39) and Cinemachine transitions (queued separately) - neither was part of tonight's task list and wasn't started.
  - Commits from tonight: `efdafa0` (Skill Tree visibility/glow/pan-zoom + Active Upgrades rework + Scribe row cleanup), `fc5965c` (Stats bonus breakdown), plus the doc/GitHub-Issues/version-bump batch. Push status: see the git log at the time you're reading this - the user's instruction was to hold `git push` until "most of these bugs" were fixed, which is now true (everything code-shaped from tonight's list is built); check whether a push already happened before assuming these commits are still local-only.
- Already confirmed during spec'ing (don't rebuild): the "Overseer's Wisdom" branch already IS the manager-bonus-boost skill the user asked for ("a skill that increases the +25% manager bonus by 5% per upgrade") — just needed the Phase A.4 cost-display fix to actually show its numbers, not new mechanic work.

## Parallel session: Bible Character Index (started 2026-08-06, OVERNIGHT AUTONOMOUS SESSION IN PROGRESS)
Same night as the build session's own overnight push above — both are running at once, per the user's explicit design (two Claude Code sessions on this project in parallel, cross-session messages used to hand off UI to-dos rather than going back to the user). Full detail lives in `E:\Unity\Clicker Genesis Documentation\Bible-Character-Index.html` (living doc, book-by-book) and `Assets/Resources/Bible/CharacterIndex/*.json` (machine-readable source of truth) — this section is a pointer, not a duplicate.
- **Hand-built and user-confirmed (Genesis through Deuteronomy, 5 books):** Genesis (19 scribe tiers, 80 character entries — the full genealogy chart, not a curated subset, per explicit user correction), Exodus (10 tiers, 23 entries), Leviticus (**REVISED** from an initial 4-person draft to a 14-tier **law/theme model** — see below), Numbers (11 tiers, 22 entries), Deuteronomy (8 tiers — 7 law + Moses' death as a person tier).
- **New pattern established: law/theme-typed scribe tiers (`role: "law"`), for books that are thin on narrative but rich in content.** Leviticus is almost entirely ritual/legal instruction, and a person-based roster for it was thin and mostly re-treaded Moses/Aaron from Exodus. Per the user's direction, tiers for a book like this are themed around the actual laws/rituals (e.g. "The Sin Offering," "The Day of Atonement") rather than forcing people into slots that don't fit. **The real test for when to use this (a direct, important user correction, apply it to every future hybrid book):** don't mechanically classify a section as law vs. narrative — understand WHY it exists, what prompted it, what lesson it's teaching, then let THAT determine the structure. The working sub-test that emerged: is a recurring person's role in this book PERSONALLY EMBODIED (they act, suffer, decide, are physically changed by events) or PURE TRANSMISSION (just the messenger)? Aaron stayed a full manager in Leviticus (personally ordained, personally grieves, personally performs Atonement); Moses in that same book became just a `deliveredBy` note on the law tiers, since he's almost entirely "the LORD spake unto Moses, saying" there — but in Deuteronomy, where the whole book is framed as Moses' own personal farewell address, most tiers are still law-typed (with Moses as `deliveredBy`) EXCEPT his death (ch. 34), which stays its own person tier as the one truly singular, personally-embodied beat.
- **This is also where the Ten Commandments live now** — Deuteronomy tier 2, re-given in full to the generation that wasn't at Sinai (Deuteronomy 5). See the Ten Commandments minigame bullet above for the related deferred idea.
- **Recurring accuracy rule, now applied across 5 books:** flag naming collisions explicitly rather than silently merging or picking one. Caught so far: two men named Lamech (Genesis 4 vs. 5), two named Nahor (Genesis 11 — one is his own grandson), two named Noah (the Flood; a woman, one of Zelophehad's daughters in Numbers 27), two named Milcah (Genesis 11:29; Numbers 27), two named Eliezer (Abraham's steward, Genesis 15:2; Moses' son, Exodus 18:4). Also: a later-book name for an unnamed figure is only used when the text ITSELF explicitly links the two mentions (Jannes/Jambres, unnamed in Exodus, legitimately named via the explicit "withstood Moses" cross-reference in 2 Timothy 3:8) — NOT when a name is merely traditionally/rabbinically associated without the text making the connection (Pharaoh's daughter in Exodus 2 is deliberately left unnamed, not asserted to be "Bithiah" from 1 Chronicles 4:18).
- **FULL OLD TESTAMENT INDEXED — 39/39 books complete (2026-08-06 overnight session, finished after the user woke briefly to say "keep going with the rest of the books").** Switched to a multi-agent Workflow (user-approved) for everything past Deuteronomy. Joshua through Job (13 books) went through draft → adversarial verify → corrective redraft, surviving a session usage-limit hit mid-pass (resumed after reset) and two malformed-agent-output books plus one dangling-reference straggler, all caught and fixed by hand. Psalms through Malachi (21 more books) followed the same pipeline — **Psalms got a hand-designed 19-tier template first** (3 author tiers + 8 theme categories + 8 standout individual psalms) rather than being delegated blind, since 150 chapters is too large to tier individually; the corrective pass then added Psalm 89 (Davidic Covenant) as its own tier and promoted Heman the Ezrahite out of a bulk list into a real entry. **All 39 books now pass a schema-consistency check** (every `managerId`/`parentManagerId` resolves to a real character, no self-contradictory manager+submanager roles, no empty character arrays) — 408 scribe tiers, 794 character entries total. Per-book HTML write-ups only exist for Genesis–Numbers so far; Deuteronomy onward is JSON-only but equally validated — not blocking, full write-up queued as follow-up. Check `Bible-Character-Index.html`'s status section for the current state.
- **Relevant to the build session specifically:** the per-character verse-reference manager-gating mechanism noted above (in Open Questions) now has real data to build against for Genesis/Exodus/Leviticus/Numbers/Deuteronomy — don't wait for all 39 OT books, the first 5 are usable now. The `role: "law"` tier type is a new concept the eventual Managers-tab UI and future genealogy/fact screen will need to handle differently from person-type managers (a law tier's "row" should show its `purpose`, not a birth/death/relationship story) — flagging now so `ManagerDefinition`/`ManagerListUI` aren't designed in a way that forecloses it.

## Open questions (ask the user, don't guess)
- Psalms: candidate starter book vs. prestige-gate book. Real KJV per-chapter data available (150 chapters, 2,461 verses, mean 16.4/chapter, median 12, shortest Psalm 117 at 2 verses, longest Psalm 119 at 176; 50 chapters under 10 verses, 8 chapters ≥40 verses) — logged in Notion. User declined to decide this 2026-07-31 ("let's not do that") — don't push it, wait for the user to bring it back up.
- Which Play Store dev account to publish under (studio/publisher name itself is resolved — see Tech setup).
- IAP scope: cosmetics-only vs. also a patronage/support tier.
- Exact scribe types, base Ink/sec rates, and manager unlock levels for the Clicker/Manager subsystem.
- XP constants are now implemented with placeholder values (see Confirmed design above) — open item is tuning them after real playtesting, not picking them from scratch.
- **Font licensing:** Georgia (currently applied everywhere) is a Microsoft-licensed system font — fine for prototyping, likely not clearable for a shipped commercial build. Need a properly licensed serif before release (open-source EB Garamond/Cormorant, or a purchased license).
- **Real scripture-accurate scribe unlock gating and further book content** — user explicitly wants to read through the actual text with Claude to verify accuracy before assigning more theming/unlock positions (beyond the current placeholder 0–7 verse-index thresholds, which only exercise the mechanism, not real narrative position). Needs more Genesis text loaded first. Don't invent this content unilaterally. **Mechanism now clarified (2026-08-06, NOT YET BUILT):** a manager should specifically require the exact verse their character first appears in to be unlocked (e.g. if a character's first mention is Genesis 8:14 and that verse hasn't been bought yet, the manager stays locked regardless of level/Ink), layered on top of (not replacing) the existing scribe-tier-must-be-unlocked-first gate. This needs real per-character verse-reference data, which the parallel "Bible indexing" session's `Assets/Resources/Bible/CharacterIndex/*.json` output is expected to supply — don't build the real gating until that data exists and has been reviewed together, same "don't invent unilaterally" rule as above.
- **Scribe output values need rebalancing (noted 2026-08-06, not yet done)** — with all the multiplier/skill-boost layers now stacked on top of the base per-tier numbers (milestone curve, progress multiplier, manager bonus, Overseer's Wisdom), the original placeholder scribe costs/outputs are increasingly likely to feel off. Revisit once there's real playtesting data, same "placeholder pending playtesting" rule as every other numeric constant in this project.

## Deferred / on hold (not open questions — do not chase these, revisit only if the user reopens them)
- NIV and Amplified translations — entire topic is on hold for a future update, not being designed around at all right now.
- **Save/persistence system (explicitly deferred 2026-07-31, user's own call — "we'll get to that saved gaming system later, one of the latter things we do"; elaborated 2026-08-06, still deferred).** Right now progress (Ink, XP, unlocked verses) only survives scene changes within a session via the `GameLoopController` singleton — it does NOT survive closing the game. Only the sound setting persists (via PlayerPrefs). Do not build save/load until the user brings it up. Scope, once it IS picked up, per the 2026-08-06 elaboration: (1) **social login** — link multiple accounts (Microsoft/Windows login, Apple/iCloud login, etc.) to one save so progress carries across platforms/devices; (2) **cloud save location** — likely device-native (iCloud on Apple, Google Drive on Android/Google), needs real platform-permission work to read/write on install, not just a local file (PC can stay a simple local save file, lower priority to solve); (3) **encryption/anti-cheat** — save data (Ink, Grace, and especially the future paid currency below) must be encrypted so cheating clients/save editors can't fabricate balances. None of this is scoped further yet — three open sub-problems, not a plan.
- **Main Menu branding/color theme — separate open question, not decided.** User's personal brand colors tend toward black/blue/silver; considering aligning with existing "NeoGentrics" (their book imprint) branding instead; hasn't settled on anything for Recon Towers Gaming specifically. Main Menu currently uses the SAME parchment/ink/gold palette as gameplay screens as a safe placeholder — user explicitly said the menu doesn't have to match gameplay theming. Revisit when they decide.
- **Submanagers (relayed 2026-08-06 from the parallel "Bible indexing" session — backlog for a full version bump, v3/v4, NOT a v1/v2 build target).** Minor Bible characters attached to a major manager, granting either a scribe-cost-reduction % or a boost to their parent manager's output bonus — exact mechanic still being designed in that other session, not specced here. Do not build; just keep the concept in mind so `ManagerListUI.cs`/`ManagerDefinition` aren't designed in a way that forecloses it later.
- **Character Genealogy/Index screens (relayed 2026-08-06, same source, same "future full version" scope — NOT v1/v2; refined 2026-08-06 same day via an addendum).** Original spec: clicking an unlocked manager opens a full-screen genealogy view — skill-tree-style layout, but descending from the selected person at top in a tournament-bracket branch pattern below, showing how characters relate. Each person shows fun facts, verse references (shown even for still-locked verses, as an unlock enticement), and ages at key scriptural events where the text states them. Meant to be translation/book-agnostic so it carries over past KJV/OT. **Refinements from the addendum:**
  - **Every character needs an entry, not just major managers** — a character who's only a submanager in one context (e.g. Melchizedek, a planned Genesis submanager under Abraham/Covenant Seal) may become a full major manager elsewhere (a later canonical book, or future custom content from other traditions like the Geneva Bible or Ethiopian/Ge'ez canon — e.g. a floated "Book of Melchizedek" expansion covering material before Genesis 1). Every character, major or minor, needs a genealogy entry/page ready to receive richer data later without restructuring.
  - **Layout leaning, not locked:** rather than one giant zoomable screen like the Grace Skill Tree, user is leaning toward each character having their own individual page/screen (reached by clicking their name) — a single screen with 60+ books' worth of figures would be unusably cluttered at that scale. Still open to reconsider.
  - **Cross-linking:** scribe hover info and manager info should link back and forth to each other's genealogy/detail screens via buttons, so a player can navigate scribe → manager → genealogy → back, discovering how figures connect as they play. User's core framing: this is effectively a "study bible clicker game" — passive Ink generation continues while the player is reading/exploring, so this depth directly serves the game's teaching goal, not just flavor.
  - Do not build any of this; noted so it isn't lost. Data layer (`Assets/Resources/Bible/CharacterIndex/*.json`, starting with Genesis) is being built now in the parallel indexing session, ahead of any of the above.
- **Scribes tab row simplification (relayed 2026-08-06, same addendum, same "not to build now" backlog scope).** Same treatment as the `ManagerListUI.cs` description-removal fix (already built, see Scribes/Managers tab split note above) but for `ScribeListUI.cs`: remove the static origin/reference/description text from each scribe row, replace with a hover tooltip on the scribe's icon showing that info instead. Explicitly grouped with the genealogy-screen backlog above (not an immediate follow-up), since it's meant to link into that same cross-navigation system once built.
- **Book-unlock knowledge-check minigame (floated 2026-08-06 by the user, idea capture only, NOT v1/v2).** Completing a short minigame testing recall of the book just finished would be required once to unlock the next book; after that first completion it stays available to replay anytime from a separate menu ("play unlocked minigames"). Explicitly floated as a possible home for the Ten Commandments specifically, but the indexing session found a better fit for those in Deuteronomy 5 (see `Assets/Resources/Bible/CharacterIndex/Deuteronomy.json` — they're re-given in full there to the generation that wasn't at Sinai, a genuinely purposeful re-statement, not just a repeat) — so the minigame stays a good future feature on its own merits, just not the fix for that specific gap. Do not build; noted so it isn't lost.
- **Store & Monetization concept (spec'd 2026-08-06, idea capture only — full detail in the offline doc folder's Store-Monetization.html, not summarized in full here).** Third currency **CONFIRMED as "Talents"** (2026-08-06, user approved — real Biblical monetary unit, Parable of the Talents, fits the Ink/Grace naming convention). Core pattern: purchasable rows (managers now, submanagers/other content later) can carry a second Talents-paid button alongside the normal Ink/Grace one, built in from the start so real IAP wiring later doesn't require touching every feature. Prominent characters may skip Ink entirely and require Grace or Talents instead of the normal path — but per the IAP-never-required guiding principle, every Talents-gated unlock must stay reachable through normal play too. Time skips (Talents-bought, wallet-held until spent, denominations 30min→1 week with a discounted week price) are the other concrete idea captured. Nothing here is built; noted so it isn't lost, and so in-progress features (Managers tab, etc.) don't get built in a shape that forecloses a second button later.

## Tech setup already done
- Unity 6.5, URP 3D Core template.
- Unity Organization / publisher name: **Recon Towers Gaming** (updated 2026-07-31, no longer the old "Dark Cancer Gaming" placeholder).
- GitHub repo `clicker-genesis`, private, `main` branch, Git LFS enabled.
- Unity-MCP plugin (IvanMurzak/Unity-MCP via ai-game.dev) installed and authorized — `.mcp.json` in this folder points to the live bridge, so a Claude Code session started in *this* directory has real Editor/scene/Play-mode access.
- Core loop v1 scaffolding done (2026-07-31): `InkWallet`, `VersePricingConfig`/`PricingCurve`, `VerseDatabase` (Genesis 1:1–10 KJV sample), `GameLoopController`, all wired into `Assets/Scenes/CoreLoopTest.unity` with a bare OnGUI test HUD — press Play there to try the tap → buy-verse loop end to end.
- XP/Leveling system implemented (2026-07-31): `XpConfig`/`LevelSystem` under `Assets/Scripts/Progression/`, wired into `GameLoopController` (awards XP on verse/chapter/book completion) and the test HUD (shows level, XP progress, prestige-eligible flag). Config asset at `Assets/Config/XpConfig.asset`.
- Real Canvas UI built for `CoreLoopTest.unity` (2026-07-31), replacing the bare OnGUI overlay (`CoreLoopTestHUD` still exists, disabled, as a fallback debug tool): `Assets/Scripts/Core/CoreLoopCanvasUI.cs` binds Text/Button elements to `GameLoopController`, styled with the confirmed parchment/ink/gold palette (no real art/sprites yet — solid-color panels + a sepia-framed verse card). Built via a one-off Editor script (`script-execute`), not hand-placed. **Important gotcha hit and fixed:** button listeners and the `OnStateChanged` subscription must be wired in `Awake()`/`Start()`, not in a one-time editor-time `Initialize()` call — C# event subscriptions don't survive Unity's domain reload on entering Play mode, so wiring them only once at edit-time silently breaks live UI updates on every subsequent Play session. If UI ever stops updating again, check this first.
- CanvasScaler uses Scale-With-Screen-Size (1080×1920 reference, match=0.5) so it scales across resolutions, but the actual reflowing multi-panel desktop layout from the "Layout Requirement" confirmed direction is NOT built yet — this is just scaling, not the full adaptive layout.
- Full canonical KJV outline (all 66 books, 1,189 chapters, 31,102 verses — real per-book/per-chapter verse counts, no verse text) saved at `Assets/Resources/Bible/kjv_outline.json` (2026-07-31) — use this for any book/chapter-length-dependent design or balancing work instead of re-deriving it.
- `game-design` reference skill installed at `.claude/skills/game-design/SKILL.md` (2026-07-31, sourced from davila7/claude-code-templates via skillsmp.com) — GDD structure, player psychology, difficulty balancing, progression-type checklist. Reference material, not an interactive workflow.
- Illustrator/collaborator-facing design doc (with diagrams) lives at `E:\Unity\Clicker Genesis Documentation\Game-Design-Overview.html` (2026-07-31) — see "External-facing documentation" below.
- **Multi-scene screen framework built (2026-07-31), the real production flow** (separate from the `CoreLoopTest.unity` dev/smoke-test scene, which still exists standalone): `MainMenu.unity` → `ClickerScreen.unity` / `SettingsScreen.unity`, and `ClickerScreen.unity` ↔ `BuyVerseScreen.unity`. All registered in Build Settings with MainMenu first (index 0) — pressing Play now starts at the menu, not gameplay.
  - **Persistent state:** `GameLoopController` is a singleton (`Instance`, `DontDestroyOnLoad`-guarded) spawned once on a `GameRoot` object in `MainMenu.unity`; survives every scene change so Ink/XP/unlocked-verse progress isn't lost navigating between screens. `EventSystem` is parented under `GameRoot` for the same reason (only spawned once, in MainMenu). No save-to-disk system yet — this is in-memory-for-the-session persistence only; save/load is explicitly deferred (see Open questions / roadmap).
  - **Navigation:** `SceneTransitioner` (persistent singleton, same pattern) + `SceneNavButton` (generic "go to this scene" component) handle screen changes. Currently a **direct, instant `SceneManager.LoadScene`** — no fade. A coroutine-driven CanvasGroup fade was built and reverted: it reliably stalled forever in this project's headless Unity-MCP automation (suspect coroutines/`Start()` not being pumped between separate automated tool calls — see the Awake/Start note below). The `TransitionOverlay` GameObject + fade `CanvasGroup` are still sitting in `MainMenu.unity` unused, ready for whoever re-adds the coroutine and tests it in a real, human-driven Play session before assuming it's broken.
  - **Screen scripts:** `ClickerScreenUI` (tap + labels), `BuyVerseScreenUI` (buy + verse reveal + labels), `SoundSettingsUI` (real working sound toggle — PlayerPrefs-persisted, controls `AudioListener.volume`, swaps an icon sprite from `UI button pack 2`), `ComingSoonButton` (generic stub-feature component — click shows a toast on a shared status label instead of navigating; used for Main Menu's Prestige and Credits buttons, which are real, present, clickable buttons for features that don't exist yet, not omitted placeholders).
  - **REAL BUG found and fixed (2026-08-01):** buttons were unresponsive to actual mouse clicks on every screen past Main Menu (tap/buy/settings — all of them, not one-off). Root cause: an earlier failed build attempt left an orphaned, non-persistent `EventSystem` GameObject at scene root instead of parented under `GameRoot` — so there was no functioning input EventSystem on any scene reached via navigation, only on whichever scene happened to have had one at edit time. Fixed by reparenting `EventSystem` under `GameRoot` in `MainMenu.unity` (verified: `EventSystem.current` is non-null and persists correctly on `ClickerScreen` now). **User confirmed via real Play testing (2026-08-01) that this fix works.** If any screen ever goes unresponsive again, check `GameLoopController.Instance.transform.childCount` first — it should be non-zero (EventSystem parented there), not 0.
- **Scribes & Managers system implemented in code (2026-08-01)**, not just designed: `ScribeDefinition`/`ScribeSetConfig`/`ScribeSystem` under `Assets/Scripts/Economy/`, wired into `GameLoopController` (`Update()` ticks passive Ink/sec production every frame; `BuyScribe(tierIndex)` spends Ink and increments owned count). `Assets/Config/GenesisScribes.asset` holds the real 8-tier Genesis roster from the earlier design (Reed Pen/Adam through Joseph's Storehouse/Joseph), with placeholder-but-real cost/output numbers (baseCost 10→500,000 across tiers, growth rate 1.15, output 0.5→2,500 Ink/s) — flagged as pending playtesting, same rule as every other numeric constant in this project.
  - **Design simplification made explicit (not silent):** the original notes described managers as "auto-triggering" a scribe's action "so you don't have to keep manually clicking it" — but scribes are purely passive Ink/sec generators (no manual click involved at all, so there's nothing to auto-trigger). Implemented instead: once a manager's player-level threshold is reached, it grants a flat **+25% output bonus** to its own tier. Still level-gated as originally specified, just mechanically coherent with a passive-production model. Revisit if the user wants literal "auto-click" behavior once there's an actual click-to-collect mechanic to automate.
  - **Clicker Screen redesigned (2026-08-01)** around this system, following user-supplied reference images (Farm Ring Idle / Harbor Tycoon / Mine Clicker style): left panel = scrollable list of scribe rows (icon, name, flavor text, live manager status, owned count + Ink/s, cost, buy button — one row per tier, built dynamically from `ScribeSetConfig` via `ScribeListUI.cs`, not hand-placed); right panel = Ink/Level HUD, a big circular Tap button with real feedback (scale-pulse + a floating "+1 Ink" text that rises and fades, both in `ClickerScreenUI.cs`), the locked Prestige button, and nav buttons. Verified end-to-end: rows build correctly, buying deducts Ink and updates owned/cost/Ink-per-second text live, manager bonus applies and displays correctly once the level threshold is crossed.
  - Two icons from a newly-added asset pack (`Assets/Homeless/UI Assets/Item_Icons/`) are in use — `UI_Icon_scroll.png` (Papyrus Scroll tier) and `UI_Icon_Journal.png` (Ark's Manifest tier), reimported Texture→Sprite like the earlier sound icons. The rest of that pack (food/potion/gem/coin icons) doesn't fit the theme and is unused.
  - **Prestige button relocated (2026-08-01, user correction):** moved off Main Menu (was wrong — user clarified Prestige is a gameplay-screen feature, locked until earned) onto `ClickerScreen`, gated by `Levels.IsPrestigeEligible` — shows "Prestige (Locked)" and is non-interactable until eligible, then becomes clickable (still just a "coming in a future update" stub, since the actual prestige flow isn't built). Main Menu still has the Credits stub.
  - **IMPORTANT environment gotcha:** in this project's headless Unity-MCP automation, `Start()` was observed to never reliably fire for components in a freshly-loaded scene (only `Awake()`, which runs synchronously as part of the scene load, is guaranteed). All screen-UI wiring (button listeners, `OnStateChanged` subscription, initial refresh) now lives in `Awake()`, not `Start()`, across every screen script including `CoreLoopCanvasUI`. This may be an automation-specific quirk rather than a real Unity behavior difference (i.e. it might not reproduce in a normal human-driven Play session) — but `Awake()` is strictly safe either way, so there's no reason to move it back without a specific reason.
- **TextMeshPro installed (2026-07-31):** already bundled in `com.unity.ugui` 2.5.0 for this Unity version (no separate package needed — `package-add com.unity.textmeshpro` fails on Unity 6 for this reason, that's expected). Essential Resources (default font) imported via `TMPro.TMP_PackageResourceImporter.ImportResources(true, false, false)`. All new screens use `TextMeshProUGUI`, not legacy `Text` — only `CoreLoopCanvasUI`/`CoreLoopTestHUD` (older, `CoreLoopTest.unity` only) still use legacy `Text`/OnGUI.
- **Removed two broken packages (2026-07-31, user-approved):** `extensions.unity.base` and `extensions.unity.ubuilder` were already in `Packages/manifest.json` before this project was touched, had missing dependencies (Sirenix/Odin Inspector — paid, never installed; UniRx — never installed), and were blocking compilation of new scripts project-wide (not just cosmetic console noise). Removed via `package-remove`. Unrelated to anything built here — if similar packages reappear, that's a preexisting project-template issue, not something introduced by this work.
- **Click Power upgrade + shared milestone-multiplier curve implemented (2026-08-01):** `Assets/Scripts/Economy/MilestoneCurve.cs` is the shared "buy 10/25/50/100 of something, it gets permanently stronger" curve (1.2×/2×/3×/4× — placeholder, pending playtesting) originally built for scribe output, now also reused for the actual tap/click value via `GameLoopController.ClickPowerLevel`/`EffectiveTapAmount`/`BuyClickPower()`. This is what the user meant by "upgrade the thing they're clicking" — a purchasable Click Power upgrade (cost 25 Ink, growth 1.15×) that scales tap value at the same breakpoints as scribe ownership. `ClickerScreenUI` has a real "Upgrade Tap" button + a "Each tap: X Ink" label.
- **Verse-progress-gated scribe unlocks implemented (2026-08-01):** each `ScribeDefinition` now has `unlockAtVerseIndex` — a tier is locked (shown with a 🔒, buy button disabled, cost hidden) until the player has bought enough verses. This is what the user meant by "buying verses unlocks the next thing to click." **Current thresholds (0–7, spread across our only 10 loaded Genesis 1:1–10 verses) are placeholder mechanism-testing values, NOT accurate to real narrative position** — Noah/Abraham/Jacob/Joseph's actual stories are thousands of verses beyond what's loaded. Real content-accurate gating needs (a) more Genesis text loaded and (b) the user's own read-through to verify accuracy, which they've explicitly said they want to do themselves before we assign real theming — don't invent further scripture-grounded content unilaterally, propose and verify together.
- **Robustness fix: `GameLoopController.EnsureBootstrapped()` (2026-08-01).** If Unity's Editor happens to be sitting on any scene other than MainMenu when Play is pressed (easy to do by accident while navigating scenes during dev), `GameRoot` never spawns and every screen fails silently (no Ink wallet, no passive income, taps do nothing) — this was very likely the real cause of an earlier "passive income isn't working" report. Every non-MainMenu screen script now calls this first in `Awake()`; if `GameLoopController.Instance` is null, it redirects to MainMenu instead of running broken. Applied to `ClickerScreenUI`, `BuyVerseScreenUI`, `SoundSettingsUI`.
- **Fantasy Wooden GUI Free imported and applied (2026-08-01, user's own asset)** — `Assets/Fantasy Wooden GUI  Free/`: parchment board panels (Large/Medium/Small), wooden text buttons (`TextBTN_Medium`/`TextBTN_Big` + pressed states), all reimported Texture→Sprite (same fix needed as every other imported icon pack). Applied across `ClickerScreen.unity` — parchment board backgrounds replace flat-color panels, wooden sprites replace flat-color buttons. Button label color had to be bumped to a light cream (was dark-on-dark-wood, unreadable) — same fix needed if more wooden buttons get built. (Superseded 2026-08-04 — see the full-backdrop re-theme bullet below; every screen now uses the stone texture, not flat color.)
- **Font fixed — Georgia now used everywhere (2026-08-01), replacing TMP's default LiberationSans.** Generated real `TMP_FontAsset`s from `C:\Windows\Fonts\georgia.ttf`/`georgiab.ttf` (copied into `Assets/Fonts/`, then `TMPro.TMP_FontAsset.CreateFontAsset(...)` — **must explicitly `AssetDatabase.AddObjectToAsset()` the resulting atlas texture and material as sub-assets**, otherwise they get garbage-collected and every text component using that font throws `MissingReferenceException` the next time anything touches it; hit this once, fixed by recreating properly). Applied to every existing `TextMeshProUGUI` across all 4 scenes (31 components) and set as `TMP_Settings` project-wide default so new text uses it automatically. **Licensing flag: Georgia is a Microsoft-licensed font bundled with Windows — fine for local prototyping/iteration, but likely NOT cleared for embedding in a shipped commercial game build.** Needs a properly licensed serif (open-source like EB Garamond/Cormorant, or a purchased license) before release — this is a placeholder like everything else numeric/visual in this project right now, just flag it as a *legal* placeholder, not just a design one.
- **Full backdrop re-theme across every screen (2026-08-04).** All four scenes (MainMenu, ClickerScreen, BuyVerseScreen, SettingsScreen) previously showed flat near-white/cream fill (or the default blue-gray URP Skybox gradient peeking through gaps) wherever there wasn't a parchment panel directly on top — read as "a bunch of parchment floating on plain color," not the intended ancient/lived-in feel. Fixed by applying `UI board Large stone.png` (same Fantasy Wooden GUI Free pack) as a full-bleed `Canvas/Background` Image on every screen, plus setting each scene's Camera to `CameraClearFlags.SolidColor` with a warm dark-brown fallback (`#35261A`) instead of Skybox. **Gotcha hit and fixed:** the stone sprite had no 9-slice border defined (`spriteBorder` was `(0,0,0,0)`), so `Image.type = Sliced` behaved like a full stretch — its baked-in frame art (fine at the source's native ~921×617) stretched ~4x too thick at large canvas sizes, visually crowding edge-anchored UI. Fixed at the source via `TextureImporter.spriteBorder = (130,90,130,90)` + reimport — any future large full-bleed sprite needs a real border set the same way, don't assume Sliced "just works" without checking `sprite.border` first.
- **Main Menu full redesign (2026-08-04).** Play/Settings buttons moved into a left column with a new `QuitButton.cs` component added below them (`Application.Quit()`, desktop-only via `GameSettings.IsResolutionSelectionSupported` — there was previously no way to close the app from the menu at all). New `DescriptionPanel` (golden-tan tinted `UI board Medium parchment`, `Color32(0xD8,0xB4,0x7E,0xFF)` tint) on the right holds game-description copy; the tagline moved into the left column above Play. Credits "i" icon re-skinned from a bare parchment square to `UI board Small stone.png` with light cream text, and inset further from the screen corner. Hit two overlap bugs while laying this out (subtitle sitting on top of the Play button from an accidental Y-position change when it should have only moved on X; description panel poking under the title banner) — both root-caused and fixed via exact `RectTransform.GetWorldCorners()` measurement, not just eyeballing a screenshot (the project's known screenshot-tool staleness issue makes eyeballing unreliable — see the MCP-bridge flakiness note further up).
- **Settings screen enlarged + reworked (2026-08-04).** Parchment `Panel` grown from 1500×760 to 1740×850 (was floating in the middle of the screen with a lot of dead space around it, especially after the stone backdrop made the gap obvious). Sound icon moved from the row's left edge to the right side next to its own Toggle button. Every right-side control (Toggle/Change/+-/Run in Background) repositioned from flush-right-edge to a fixed offset closer to its label, and widened from 220 to 340px so `RunInBackgroundLabel`'s "Run in Background: On" fits on one line.
- **ClickerScreen/BuyVerseScreen top-edge clearance fix (2026-08-04).** After the stone backdrop went in, the Ink/XP bar and left parchment panel on both scenes sat only 6–20 canvas units from the true top edge (BuyVerseScreen's bare `InkLabel` text — no background plank unlike ClickerScreen's — was visibly clipped by the frame). Pushed both down to a consistent ~40-unit gap.
- **Scribes tab header redesign (2026-08-04).** The `Title` text on `ClickerScreen`'s Scribes/Managers panel was static "Scribes" and never updated when switching to the Managers tab; the scribe bulk-buy multiplier button (`ScribeMultiplierButton`) sat alone in the top-right corner, always visible on both tabs since it lived outside `ScribeListRoot`. Fixed: `ClickerScreenUI.SetTab()` now drives a new `ScribesHeaderLabel` (text becomes "Multiplier" on Scribes, empty on Managers) and toggles a new `ScribeMultiplierButtonRoot` field — both added as public fields, wired to the existing `ScribeMultiplierButton` GameObject. Also centered each scribe row's icon vertically (`RowTemplate/Icon` was top-anchored) and added left/right padding so icons and Buy buttons aren't touching the scroll panel's edges.
- **Prestige button visibility + layout (2026-08-04).** New `XpConfig.prestigeButtonVisibleLevel` field (default 2) drives `LevelSystem.IsPrestigeNear` — the button now appears (locked) starting at level 2 per explicit user request, rather than the old `PrestigeLevelThreshold - 1` (which was level 4, one below the level-5 unlock). Also moved from a lone fixed-320×90 button above Upgrade Tap to sit directly beside `GoToBuyButton` ("Buy Verses") as a matched 440×110 pair.
- **Manager auto-buy (2026-08-04).** Reopens the "Design simplification made explicit" note above — user explicitly asked for managers to also auto-purchase their scribe tier, not just boost output. `GameLoopController.Update()` now loops every tier each frame: if `Scribes.IsManagerUnlocked(i)` and the tier is unlocked and affordable, auto-buys one unit (plain arithmetic, no allocations/UI work — verified this doesn't reintroduce the bug #22 per-frame-UI-rebuild lag).
- **Run in Background setting + real freeze bug fix (2026-08-04).** User found via live desktop testing that the whole game (Update loop, passive Ink) froze the instant the window lost focus — confirmed root cause was `PlayerSettings.runInBackground` defaulting to `False`. Set `True` at the build level, plus a new `GameSettings.RunInBackground` (default `true`, PlayerPrefs-backed) runtime opt-out toggle in Settings, per the user's explicit ask that it not be silently forced on with no player control.
- **Queued, not started: Cinemachine-driven transitions (2026-08-04).** User wants the Claude skills from `github.com/Nice-Wolf-Studio/unity-claude-skills` installed so the already-installed Cinemachine package can drive thematic scene-transition/loading animations (and eventually sound) — wants a "cinema" feel, not an instant cut. Explicitly fine to pick up after the current polish pass finishes.
- **Krita and GIMP installed locally (2026-08-06)** — available for future custom asset creation (icons, character art, etc.) if/when needed. User flagged this in passing, explicitly "not important right now" — no Claude skill installed for either yet; revisit if a task actually needs them.

## Working style notes
- Keep the Notion page in sync with any design decisions made in this session — it's the cross-session source of truth, not this file's job to duplicate long-term. Notion stays private to the user (not shared with collaborators).
- **Notion structure (restructured 2026-07-31, user's explicit request):** the hub page (linked above) is a short index only — Concept, Guiding principle, and a linked "Contents" nav. All actual design content lives in 11 subpages: Core Loop & Economy, Progression: Books/Testament/Prestige, XP & Leveling, Scribes & Managers, Art & UI Direction, Translations, Future Systems & Idea Backlog, Assets & Credits, Open Questions & Status, Project Setup & Tech, Changelog. When logging a new decision, edit the relevant subpage directly — do NOT dump new content back onto the hub page. Fetch the hub page first if you don't have current subpage URLs.
- **Asset/credit tracking (started 2026-07-31, user's explicit request):** any third-party asset (Asset Store package, font, audio, etc.) that actually gets *used* (wired into something, not just imported) must be logged in the Notion "Assets & Credits" subpage AND mirrored in the doc folder — what it is, what it's used for, and its attribution/license status. Update this every time a new asset gets put to use, as part of the same changelog-worthy work, not as an afterthought. **Status as of 2026-08-01:** `UI button pack 2` — `SOUND-BLACK.png`/`sound-off-black.png` drive the Settings sound toggle icon (Texture→Sprite reimport needed, same as every pack below). `Homeless` — `UI_Icon_scroll.png`/`UI_Icon_Journal.png` used on two scribe rows. `Fantasy Wooden GUI Free` (user-imported 2026-08-01) — parchment board panels + wooden buttons, applied across `ClickerScreen.unity`. `Quantum Tek` still imported but unused (its scene-transition framework wasn't needed for the hand-rolled nav system built instead). `UI button pack 3` still imported, unused. **Recurring gotcha: every image asset imported into this project so far has needed its Texture Importer type manually switched from Texture2D to Sprite before it's usable in UI — check this first whenever a newly imported image "doesn't show up" as a Sprite.**
- **User explicitly dislikes Notion tables ("cell sheets")** — prefers infographics. Where a table would naturally go (comparisons, tier lists, changelog), build a small SVG infographic instead and upload it via `notion-create-attachment` (SVG content works directly, returns a `file-upload://` source to embed as `![caption](file-upload://...)`), or use `<callout>` blocks / bulleted lists for anything too simple to warrant a real diagram. The Changelog subpage uses `<details><summary>` toggles per version instead of a table.
- **External-facing documentation** (explainers, outlines, diagrams for the user's illustrator/collaborators) goes in `E:\Unity\Clicker Genesis Documentation\` — a separate folder outside this repo, NOT the Notion page and NOT this CLAUDE.md. Keep it updated alongside Notion when design changes — check this folder is actually current before assuming it is, it has fallen behind before (2026-07-31 incident: art direction + XP + scribe/manager content landed in Notion but not here until the user asked).
- **Bug tracking process (established 2026-08-01, corrected 2026-08-03 per user's explicit process correction, extended 2026-08-06):** every bug the user reports gets logged with an increasing bug number in the Notion "Bug Tracker" subpage AND the offline `E:\Unity\Clicker Genesis Documentation\Bug-Tracker.html`, both laid out as **two columns — Open (not yet confirmed) and Fixed (confirmed)**. A bug moves from Open to Fixed **only when the user explicitly confirms it themselves** — not when the code changes, not when an automated script check passes, not from reading a screenshot without them saying so. **New (2026-08-06, user's explicit standing rule): every bug also gets a real GitHub Issue on `neogentrics/Clicker-Genesis`, kept in sync with Bug-Tracker.html on every push/release** — closed = user-confirmed Fixed, open = still in the Open column (including "pending confirmation" entries). Applies to any GitHub repository going forward, not just this one. **Versioning correction (2026-08-03):** the feature version (Y in X.Y.Z) does NOT bump just because a feature-shaped chunk of work landed — it only bumps once the user confirms the current feature set is genuinely playable end to end. Until then, every fix — no matter how much code it touches — is a Z-patch under the current Y. (0.3.0/0.3.1/0.4.0 were logged in error on 2026-08-01 and renumbered back to 0.2.2–0.2.4.) **Current version: 0.3.0**, bumped 2026-08-06 on the user's own direct overnight instruction ("we can officially move to update three") — this is a deliberate override of the versioning rule above, not a claim the rule's conditions were independently met (many bugs are still unconfirmed); see the version banners in Bug-Tracker.html/Roadmap.html for the full judgment-call note. Log a new bug batch every time the user reports issues from real Play testing — don't just fix silently, and don't mark anything Fixed without their word.
- **Project root folder renamed 2026-08-01** from `E:\Unity\Clicker Genesis\` (with spaces) to `E:\Unity\Clicker_Genesis\` (underscore) — Unity itself warned the space in the path could cause issues with AI Game Developer tooling. The external doc folder (`E:\Unity\Clicker Genesis Documentation\`) keeps its original name with spaces; only the main project folder changed. Use the underscore path for all shell/bash operations going forward.
- **Any SVG dropped in `E:\Unity\Clicker Genesis Documentation\`** must also be converted to PNG via `svg_to_image.py` in that same folder (run with the Python 3.13 install specifically — `"C:\Program Files\Python313\python.exe" svg_to_image.py` — 3.14 on this machine is broken) so the user can view it without a browser. Do this every time, not just when asked.
- Don't add scope (per-book unique mechanics, achievements, etc.) beyond what's marked v1 above without the user asking.
